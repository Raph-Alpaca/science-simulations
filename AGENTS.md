# 공통 작업 규칙

1. README, docs/OWNER_INPUT, SYSTEM_SPEC, STATUS와 실제 Git 상태부터 읽는다. 기존 파일을 무조건 재작성하지 않는다.
2. 현재 요청된 단계만 수행한다. 계획·코드 작성·모의 시험·실제 배포를 분리해 보고한다.
3. 웹 검색·브라우저·하위 에이전트의 실제 연결을 확인한다. 도구가 없으면 있다고 가장하지 않는다.
4. 교육과정·교과서·출처는 실제 확인한 부분만 검토 완료로 기록하고 미확인은 보류한다.
5. 과학적 정확성·학습 설계·실제 동작을 나누어 검사한다. 모형 가정과 한계를 표시한다.
6. 콘텐츠 작업의 변경 범위를 제한한다. 검사·워크플로·공통 코드·권한 변경은 별도 시스템 작업이다. 이 규칙은 코드와 권한으로도 강제한다.
7. 실행하지 않은 검사를 통과라고 하지 않고 실패 검사를 삭제해 통과시키지 않는다.
8. 반복·시간·호출·작업량 상한을 실제 적용한다. 생성 코드에 키·쓰기 토큰·DB 관리자 권한을 주지 않는다.
9. .local·.env·개인 메모·비공개 원문·원본 음성은 공개 Git/로그/PR/배포물에 넣지 않는다.
10. 제목과 ID를 분리하고 기존 콘텐츠와 주소를 보존한다. 중복 전송·재실행을 안전하게 처리한다.
11. 승인·판정은 해당 콘텐츠 버전에 결합한다. 실제 게시 확인 전에는 완료라고 하지 않는다.
12. 유료·공개·파괴적 변경의 범위와 영향을 확인하고 docs/STATUS에 증거와 미검증 사항을 남긴다.

이 파일은 지침이지 자체 보안 장치가 아니다.
